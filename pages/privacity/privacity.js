// Define o título da página
var pageTitle = 'Politica de Privacidade';

$(document).ready(runPage);

function runPage() {

    // Altera o título da página
    setTitle(pageTitle);

    console.log('Executando a Politica de Privacidade');

}